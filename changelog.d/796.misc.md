Releases now publish to npm via Trusted Publishing (OIDC) instead of a long-lived `NPM_TOKEN`. The release workflow proves its identity to npm with a short-lived GitHub-issued token exchanged at publish time, so there is no stored npm credential to leak, expire, or rotate — the failure mode that broke the v2.3.1 release twice. Publishing this way also generates provenance attestations automatically.
