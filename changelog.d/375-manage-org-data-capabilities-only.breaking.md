The `manageOrgData` MCP tool is now capabilities-only. Organizational patterns and policies are managed via the `manageKnowledge` tool (ingest / search / deleteByUri) with automatic AI classification. The `pattern` and `policy` `dataType` values, and the `create`/`analyze` operations, have been removed — requests using them now return a validation error pointing to `manageKnowledge`.
