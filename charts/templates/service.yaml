apiVersion: v1
kind: Service
metadata:
  name: dot-ai
  labels:
    app: dot-ai
    component: mcp-server
spec:
  type: ClusterIP
  selector:
    app: dot-ai
    component: mcp-server
  ports:
  - port: 3456
    targetPort: 3456
    protocol: TCP
    name: http